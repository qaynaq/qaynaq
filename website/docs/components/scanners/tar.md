# Tar

Reads each file inside a tar archive as a separate message. Combine with the [Decompress](/docs/components/scanners/decompress) scanner to handle `.tar.gz` or `.tar.zst` archives.

This scanner takes no configuration.

## Metadata

| Field | Description |
|-------|-------------|
| `tar_name` | Name of the file inside the archive |
