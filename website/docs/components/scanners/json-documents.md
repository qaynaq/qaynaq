# JSON Documents

Consumes a stream of one or more JSON documents. Each top-level value becomes a separate message. Works with concatenated JSON, JSON streams, and single-document files.

This scanner takes no configuration.
